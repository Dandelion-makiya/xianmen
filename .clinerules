# 项目临时文件规则

1. 禁止在本项目目录（E:\xianmen）及其子目录中创建任何 `.tmp` 临时文件。
2. 执行命令、生成文件、调试或写日志时，不得把临时文件写到项目目录；需要临时文件时使用系统临时目录，并在使用后删除。
3. 如果发现项目目录中出现 `.tmp` 文件，必须立即清理，且不得恢复或重新创建。
4. 所有脚本和构建流程都不得生成 `*.tmp`、`*.bak`、`*~` 等临时残留文件。
5. 清理临时文件时只删除 `E:\xianmen` 下的 `.tmp` 文件，不删除策划文档、代码、资源或配置文件。

